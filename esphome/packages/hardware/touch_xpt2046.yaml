# =============================================================================
# Package: Touch Controller - XPT2046 (Original CYD)
# Purpose: XPT2046 resistive touch controller initialization
# Dependencies: packages/hardware/spi_touch.yaml (requires 'touch' spi component)
# Exposes: touchscreen component 'touch_controller'
# Hardware: XPT2046 on separate SPI bus from display
# =============================================================================

touchscreen:
  - platform: xpt2046
    id: touch_controller
    spi_id: touch
    cs_pin: ${touch_cs}
    interrupt_pin: ${touch_irq}
    threshold: 400
    calibration:
      x_min: 280
      x_max: 3860
      y_min: 340
      y_max: 3860
    transform:
      swap_xy: true
    # Reset idle timer on any touch (T126)
    on_touch:
      - script.execute: reset_idle_timer
